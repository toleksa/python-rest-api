{{- if .Values.monitoring.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-dashboard
  namespace: monitoring
  grafana_dashboard: 1
data:
  config.json: |-
{{ .Files.Get "python-rest-api.json" | indent 4}}
{{- end }}
