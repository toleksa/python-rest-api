{{- if .Values.monitoring.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "python-rest-api-www.fullname" . }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ include "python-rest-api-www.fullname" . }}
      app.kubernetes.io/name: {{ include "python-rest-api-www.fullname" . }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  endpoints:
  - interval: 30s
    port: "http"
    path: /metrics
{{- end }}
